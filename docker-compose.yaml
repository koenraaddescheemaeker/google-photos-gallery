services:
  app:
    build: .
    container_name: forcekes-app
    restart: always
    environment:
      DB_HOST: supabase-db
      DB_PORT: 5432
      DB_NAME: postgres
      DB_USER: postgres
      DB_PASSWORD: YOUR_PASSWORD
    labels:
      - "coolify.managed=true"